#include <stdio.h>

int main()
{
    int nota = 50;

    if (nota >= 60){
        printf("Parabéns, você passou!");
    } else {
        printf("Infelizmente, você não passou!");
    }

    return 0;
}